#include <iostream>
#include <sstream>
#include <vector>
#include <string>

/*
int main(){
  std::string input,chunk;
  std::vector<std::string> stack;
  
  std::getline(std::cin,input);
  std::stringstream ss(input);
  while (!ss.eof()){
    ss >> chunk;
    stack.push_back(chunk);
    }
  for (std::vector<std::string>::const_iterator itr=stack.begin();itr!=stack.end();itr++)  std::cout << *itr << std::endl;

  return 0;
}
*/
